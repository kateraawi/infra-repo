apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: apps-repo
  namespace: flux-system
spec:
  interval: 2m
  sourceRef:
    kind: GitRepository
    name: apps-repo
  path: "./apps"
  prune: true
  timeout: 1m
